# ── Android 构建产物 ──
build/
app/build/
.gradle/
.gradle_build/
*.apk
*.aab

# ── 命令行构建临时目录（避开 Studio 锁冲突用，本机可删）──
.gradle_home_cli*/
.gradle_cli*/
.gradle_tmp/
app/build_cli/

# ── 本机环境（绝不上传！）──
local.properties

# ── IDE 配置 ──
.idea/
*.iml
.DS_Store
captures/

# ── 缓存与日志 ──
*.log
.kotlin/

# ── 签名文件（安全！）──
*.jks
*.keystore

# ── 签名配置（安全！）──
keystore.properties
