# 搜题助手 · 开发笔记

> 记录搜题助手的技术方案与架构设计，便于复习与迭代。

## 🧠 整体架构

```
┌─────────────┐     ┌──────────────────────────────┐
│  浮窗 UI     │     │      FloatWindowService       │
│  (Compose)  │     │                              │
└─────────────┘     │  ┌────────────────────────┐   │
                    │  │ 截屏管线                │   │
┌─────────────┐     │  │ MediaProjection →      │   │
│  主页        │────▶│  │ VirtualDisplay →       │   │
│  MainActivity│     │  │ ImageReader → Bitmap   │   │
└─────────────┘     │  └───────────┬────────────┘   │
                    │              ▼                │
                    │  ┌────────────────────────┐   │
                    │  │ OCR 管线                │   │
                    │  │ ML Kit 中文识别         │   │
                    │  │ (Hani_ctc 离线模型)     │   │
                    │  └───────────┬────────────┘   │
                    │              ▼                │
                    │  ┌────────────────────────┐   │
                    │  │ 搜题管线                │   │
                    │  │ 题干提取 → LCS 匹配     │   │
                    │  │ → 渲染结果卡片          │   │
                    │  └────────────────────────┘   │
                    └──────────────────────────────┘
```

## 🎯 核心设计决策

### 1. 长期 VirtualDisplay（解决 MediaProjection 保护机制）

**问题**：频繁创建/销毁 VirtualDisplay 会触发 Android 的 MediaProjection 保护机制，导致截屏失败。

**方案**：`startContinuousScan()` 时创建一次 VirtualDisplay + ImageReader，持续复用；`stopContinuousScan()` 才释放。

### 2. OCR 截屏范围 = 绿框（recognizeArea）真实坐标

**方案**：给绿框 View 设置稳定 ID（`R_ID_RECOGNIZE`），每次 OCR 用 `getLocationOnScreen()` 获取**真实屏幕坐标**：

```kotlin
val recogView = root?.findViewById<View>(R_ID_RECOGNIZE) ?: return
val loc = IntArray(2)
recogView.getLocationOnScreen(loc)
val rectL = loc[0]; val rectT = loc[1]
val rectR = rectL + recogView.width
val rectB = rectT + recogView.height
```

**注意**：不要用 `coerceAtMost(screenW/H)` 裁剪！浮窗可能拖到屏幕边缘（绿框部分出屏），裁剪会让 OCR 截屏和绿框错位。

### 3. LCS 最长公共子串搜题（解决 OCR 截短问题）

**问题**：`stem.contains(token)` 在 OCR 截短的题干上失效（token 比题库题干长）。

**方案**：LCS（Longest Common Substring）DP 算法：

```kotlin
// 完全包含 +100 分
if (query.contains(stem) || stem.contains(query)) score += 100
// LCS 长度加分（>= 3 才计）
val lcs = lcsLen(query, stem)
if (lcs >= 3) score += lcs
```

OCR 截短题干（如"四氢呋喃的爆炸体积分数极"）也能匹配题库完整题干（"四氢呋喃的爆炸体积分数极限范围是（）"）。

### 4. 浮窗 UI 三态按钮

```
🔓授权 →（授权成功）→ ⏸暂停 →（点击）→ ▶继续 →（点击）→ ⏸暂停 ...
```

- **暂停** = `stopContinuousScan()`（保留 MediaProjection token）
- **恢复** = `startContinuousScan()`（无需重新授权）
- **完全关闭** = 主页「关闭服务」按钮（stopService + 清 OcrBridge）

### 5. 状态同步（主页按钮实时更新）

Service 在 `onCreate` / `onDestroy` 写 `SharedPreferences("souti_state")`，主页 `LaunchedEffect` 每 500ms 轮询，实现"启动服务 ↔ 关闭服务"按钮实时切换。

### 6. 老板键（最小化）

`WindowManager.LayoutParams.height` 在 `dp(520)` ↔ `dp(40)` 间切换，浮窗缩成可拖动小条，Service 与 MediaProjection 保持运行。

## 🧩 关键类职责

| 类 | 职责 |
|---|---|
| `FloatWindowService` | 浮窗生命周期、OCR 循环、搜题、结果渲染 |
| `MainActivity` | 主页、服务控制、题库管理入口、授权流程 |
| `OcrBridge` | MediaProjection 单例桥（object），跨 Activity/Service 共享 |
| `OcrHelper` | Activity 侧授权流程封装 |
| `QuestionBank` | 题库 CRUD、txt 解析、LCS 搜索 |

## 🔗 数据流（一次 OCR 扫描）

1. `scanRunnable`（每 2 秒）→ `captureAndProcessOnce()`
2. `getLocationOnScreen` 获取绿框坐标 → 截屏裁剪
3. ML Kit `process(inputImage)` → OCR 文本
4. `processOcrText` 按"答案"切分段落 → 提取题干（括号前）
5. `bank.search(q)` → LCS 打分排序 → 取前 5
6. `renderScanResults` 渲染结果卡片（题干 + 选项 + ✔答案 + 来源）

## 🎫 题库格式（txt）

```
题干文字（）
A. 选项一
B. 选项二
C. 选项三
D. 选项四
答案:C

（空行分隔下一题）
```

解析规则：`答案[:：]` 行识别答案，`^[A-Z][.、)）]` 识别选项，其余为题干。
