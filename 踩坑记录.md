# 搜题助手 · 踩坑记录

> 记录开发过程中踩过的坑与解法，避免重蹈覆辙。

---

## 🐛 1. Kotlin Service 字段初始化器 NPE

**现象**：启动浮窗 → APP 闪退。

**日志**：
```
java.lang.NullPointerException: getResources() on null object reference
    at FloatWindowService.dp(FloatWindowService.kt:94)
    at FloatWindowService.<init>(FloatWindowService.kt:633)
```

**根因**：字段初始化器里调用 `dp(150)`，但此时 Service 还没 `onCreate()`，Context 为 null。

```kotlin
// ❌ 错误：字段初始化器调 Context 方法
private var ocrRecognizeHeight: Int = dp(150)

// ✅ 正确：字段默认 0，onCreate 里赋值
private var ocrRecognizeHeight: Int = 0
override fun onCreate() {
    super.onCreate()
    ocrRecognizeHeight = dp(150)
}
```

**教训**：**Kotlin Service/Activity 字段初始化器不能调用任何需要 Context 的方法**（`dp()`、`getString()`、`getColor()` 等）。

---

## 🐛 2. LayoutParams 类型 Cast 闪退

**现象**：拖 ◢ 调整绿框大小 → 闪退。

**根因**：绿框（recognizeArea）是 **LinearLayout 子 View**，但代码 cast 成 `FrameLayout.LayoutParams`：

```kotlin
// ❌ 错误：ClassCastException
val lp = dragArea.layoutParams as FrameLayout.LayoutParams

// ✅ 正确：匹配实际父布局
val lp = dragArea.layoutParams as LinearLayout.LayoutParams
```

**教训**：`layoutParams as XxxLayoutParams` 必须匹配 **View 的父容器类型**，不是随意选的。

---

## 🐛 3. 浮窗 UI 与 OCR 截屏错位（红框 ≠ 绿框）

**现象**：红色调试框与绿色识别框不重合，越拖浮窗偏差越大。

**根因**：
1. OCR 截屏用**手动计算坐标**（`p.x + paddingPx`），但绿框在 LinearLayout 内还有 topSpace 等偏移 → 计算不准
2. `coerceAtMost(screenW/H)` 裁剪把绿框 clip 到屏幕内，但绿框实际可能出屏（浮窗拖到边缘）→ 错位

**解法**：
```kotlin
// ✅ 用 getLocationOnScreen 获取真实屏幕坐标
val loc = IntArray(2)
recogView.getLocationOnScreen(loc)
// ✅ 不裁剪！出屏让 MediaProjection 自动处理
val rectL = loc[0]; val rectT = loc[1]
val rectR = rectL + recogView.width
val rectB = rectT + recogView.height
```

**教训**：**浮窗内 View 的屏幕坐标一律用 `getLocationOnScreen()`**，不要手动累加计算（padding/topSpace/margin 太多容易算错）。

---

## 🐛 4. 旧 OCR 异步回调错位渲染（像"两个程序在跑"）

**现象**：用户感觉"有两个程序同时在跑"，识别位置不同步。

**根因**：OCR 是异步的。每 2 秒发起一次截屏 OCR，但上一次的 OCR 回调可能还没完成，**旧回调结果覆盖新结果**。

**解法**：序列号机制，过期回调直接丢弃：
```kotlin
@Volatile private var ocrSeq: Int = 0

// captureAndProcessOnce 开头
val mySeq = ++ocrSeq

// OCR 回调里
if (mySeq != ocrSeq) return@addOnSuccessListener  // 丢弃旧回调
```

**教训**：**异步回调一定要带版本/序列号**，防止乱序覆盖。

---

## 🐛 5. 命令行 Gradle 被 Android Studio 锁死

**现象**：命令行 `gradle assembleDebug` 报 `native-platform.dll` 加载失败。

**根因**：Android Studio 开着项目时会锁住 `~/.gradle`（daemon 冲突），命令行 Gradle 无法启动 native 服务。

**解法**：
- **最快**：让用户在 Android Studio 里点 ▶ Run（Studio 自己管理构建）
- 命令行备用：删 `native` 缓存 + 独立 `GRADLE_USER_HOME` + 独立 project-cache-dir

**教训**：**Android Studio 开着时不要用命令行 Gradle 构建同一项目**。

---

## 🐛 6. 停止扫描后按钮 5 秒禁用（点开始没反应）

**现象**：点"⏹停止扫描"后，再点"🔄开始扫描"没反应。

**根因**：授权流程里 `btn.isEnabled = false`（5 秒保护），但**停止扫描分支忘了恢复 enabled**。

**解法**：停止扫描时立即 `btn.isEnabled = true`。

**教训**：**任何禁用按钮的流程，必须在对应分支恢复 enabled**。

---

## 🐛 7. ✕ 按钮 stopSelf 导致 MediaProjection 失效

**现象**：点 ✕ 后重新启动浮窗，点"开始扫描"没反应（无录屏红条）。

**根因**：`stopSelf()` 销毁 Service + 释放 MediaProjection token。下次启动 Service 拿到旧 token（已 stop）→ `createVirtualDisplay` 抛 IllegalStateException。

**解法**：
- ✕ 改 **—（老板键）**：只隐藏浮窗，**不销毁 Service**（MediaProjection 保持有效）
- 完全关闭交给主页「关闭服务」按钮（stopService + `OcrBridge.mediaProjection = null`）

**教训**：**MediaProjection token 生命周期必须和 Service 一致**。Service 销毁前必须释放，否则残留 token 会坑下次启动。

---

## 🐛 8. 浮窗拖到屏幕边缘被系统限制

**现象**：浮窗往下拖到某位置就拖不动了。

**根因**：系统默认限制浮窗不能超出屏幕边界。

**解法**：加 `FLAG_LAYOUT_NO_LIMITS`：
```kotlin
WindowManager.LayoutParams.FLAG_NOT_FOCUSABLE or
        WindowManager.LayoutParams.FLAG_LAYOUT_NO_LIMITS
```

---

## 🐛 9. OCR 搜题搜不到（contains 失效）

**现象**：OCR 识别看起来正常，但搜不到题；缩小绿框（只截题干）反而搜不到。

**根因**：`scoreMatch` 用 `stem.contains(token)`，但 OCR 截的题干**比题库题干短**（截断）：

```
OCR 截: "四氢呋喃的爆炸体积分数极"（11字符）
题库:  "四氢呋喃的爆炸体积分数极限范围是（）"（13字符）
stem.contains("四氢呋喃的爆炸体积分数极") = false → score = 0
```

**解法**：改用 **LCS（最长公共子串）** 算法：
```kotlin
if (query.contains(stem) || stem.contains(query)) score += 100
val lcs = lcsLen(query, stem)  // DP 算法 O(m*n)
if (lcs >= 3) score += lcs
```

**教训**：**模糊匹配用 LCS/编辑距离，不要用 contains**。OCR 文本天然不完整。

---

## 🐛 10. `root` 在函数参数里被当成 val

**现象**：编译报错 `val cannot be reassigned`。

**根因**：`buildStandbyUi(root: ViewGroup)` 的参数 `root` 是 **val**，闭包里 `root = null` 报错（想改的是**类字段** `root`）。

**解法**：用 `this@FloatWindowService.root = null` 显式引用类字段。

**教训**：**闭包里赋值类字段时，注意参数名和字段名不能混淆**。

---

## 🐛 读屏搜题踩坑集（v0.7 稳定版）

> 读屏搜题（全屏 OCR 多题识别）是踩坑最多的模块，单独成章。核心教训：**先看真实日志，别盲改**。

### 坑 11. ML Kit 大图静默失败（返回空，不报错）

**现象**：读屏 OCR 状态栏显示"OCR 中... diff=0%"但预览永远空，桌面能识别、文档识别不到。

**根因**：全屏 1280x2856 bitmap 直接喂 ML Kit 中文识别 → **静默失败**（不抛异常、不回调 failure，返回空文字）。之前误以为是"WebView 渲染"或"VirtualDisplay 冲突"，浪费数小时。

**解法**：OCR 输入**必须缩放**（裁剪中央后缩到 720~1080 宽），不能直接用原图。

### 坑 12. OCR 缩放 720 太小 → 错字连篇

**现象**：OCR 识别出"四氢味喃"（应为"呋喃"）、"体积分教"（应为"分数"）、"范国"（应为"范围"）。

**根因**：13sp 正文缩到 720 宽后仅 14.6px 高，**远低于 ML Kit 中文要求 ≥24px**，识别错字 + 漏题号。

**解法**：缩放到 **1080 宽**（文字 21.9px，接近下限），识别率显著提升。

### 坑 13. 题号小字 OCR 漏识别 → 改用"答案："锚点切分

**现象**：多题模式触发不出来（一直显示"1题"）。

**根因**：题号"1. 2. 3."是更小的字，OCR 常漏识别 → 题号正则匹配不到任何题号 → 切分失败。

**解法**：切分锚点从"题号"改成 **"答案：X"**（文字更大、格式清晰，OCR 稳定识别）：
```kotlin
val parts = text.split(Regex("答案\\s*[:：]"))
```

### 坑 14. 结果区不涂白 → 卡片文字污染下一轮 OCR

**现象**：题号错位（第1题卡片显示屏幕第4题）、模式不停切换。

**根因**：小窗结果区卡片文字（"🎯 题干... 答案:D"）没涂白，被下一轮 OCR 识别 → 混入 orderedText → 切分错乱 + 每次 OCR 文本都变 → 模式抖动。

**解法**：**全窗涂白**（结果区文字必须抹掉），挡题问题靠"小窗默认右下角 + 可拖动"解决，不是缩小涂白。

### 坑 15. 去重吞题 → 同前缀题误判重复

**现象**：受限空间/能量隔离等"依据《...规定》"系列题，第2、3题被判"重复"不显示。

**根因**：这些题有 22 字相同前缀（"依据《...管理规定》"），LCS 匹配前缀占优 → 都匹配到同一题库题 → 按 question.id 去重吞掉。

**解法**：**彻底去掉去重**——宁可重复显示，也不吞题（读屏无手动兜底，考试禁切应用）。

### 坑 16. 前缀硬编码脆弱 → 通用方案

**现象**：题干提取硬编码去"依据/根据《...》"前缀，但下份题库可能用"我司""在"等写法。

**解法**：改成**通用正则**，去掉"开头到第一个《...》结束"：
```kotlin
.replace(Regex("^[^《]*《[^》]*》"), "")
```

### 坑 17. 题干提取截断括号前 → 括号在开头的题匹配不到

**现象**："下列（ ）是线型橡胶"匹配不到。

**根因**：取"第一个括号前"= "下列"（2字），太短，LCS 匹配不到题库题。

**解法**：改成**去掉括号及其内容**（"下列（ ）是线型橡胶"→"下列是线型橡胶"），并让 scoreMatch 比较前也去括号空格。

### 坑 18. 选项文字混入题干

**现象**：题干提取"去括号"后，选项文字（"A.活化剂"）还在题干里。

**解法**：先截取"第一个选项字母（A-E+标点）"**之前**的内容，再去括号。

---

## 📌 通用经验总结

1. **Service/Activity 字段初始化器**：别调 Context 方法
2. **LayoutParams cast**：匹配父容器类型
3. **浮窗坐标**：一律 `getLocationOnScreen()`，别手动算
4. **异步回调**：带序列号，丢弃过期结果
5. **MediaProjection**：生命周期跟 Service 绑定，销毁要清理
6. **模糊匹配**：用 LCS/编辑距离
7. **禁用按钮**：每个分支都要恢复 enabled
8. **命令行 Gradle**：Studio 开着别用
